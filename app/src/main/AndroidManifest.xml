<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.by5388.viewpager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MultiFragmentActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/hello_viewPager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ViewPagerFragmentActivity"
            android:label="@string/fragment_with_view_pager" />
        <activity
            android:name=".ViewPagerActivity"
            android:label="@string/view_pager_image" />
    </application>

</manifest>